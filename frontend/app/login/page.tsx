// ABOUTME: Login page.
import { LoginForm } from "./LoginForm";

export default function LoginPage() {
  return (
    <div style={{ padding: "1.5rem", minHeight: "100vh", display: "flex", alignItems: "flex-start", paddingTop: "3rem" }}>
      <LoginForm />
    </div>
  );
}
